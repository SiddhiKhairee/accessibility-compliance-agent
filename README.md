# accessibility-compliance-agent
An agentic system that crawls real websites, uses a multi-agent LangGraph workflow to detect and reason about WCAG accessibility violations by business impact, generates and verifies code fixes, requires human approval before any pull request, and tracks its own performance and cost — culminating in a real PR to an open-source project.
